// To  increment a number means to add 1 to it. The increment operator is a shortcut to adding 1 to a variable.
var myVar = 87;
myVar = myVar + 1; // or myVar++; which is the same as myVar = myVar + 1;