// A do-while loop is similar to the while loop but ensures the
// block of code runs at least once before checking the condition.

let number = 0;

do {
  console.log("Number is: " + number);
  number++;
} while (number < 3);