// The remainder operator looks like this: (%) a percent sign. It returns the remainder of dividing two numbers.

var remainder;
remainder = 11 % 3; // remainder is equal to 2
console.log(remainder);
// sometimes remainder is used to check if a number is even or odd.