// equality operators are used to compare two values and return a boolean value
// == is the equality operator

function isEqual(val)
{
    if (val == 15)
    {
        return "Equal";
    }
    return "Not Equal";
}

console.log(isEqual(10));