// you can manipulate Arrays with push () method
// push () method adds one or more elements to the end of an array and
// returns the new length of the array.

// Example
var arr = [1, 2, 3];
arr.push(4);
console.log(arr);