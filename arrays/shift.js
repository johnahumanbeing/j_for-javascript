// you can use the shift method to remove the first item from an array
// and return it. This method changes the length of the array.

const arr = [1, 2, 3, 4, 5];
const firstItem = arr.shift();

console.log(firstItem);