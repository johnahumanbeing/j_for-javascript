// youu can use the pop() method to remove the last element from an array.
// pop() method removes the last element from an array and returns that element.

// Example
var arr = [1, 2, 3];
arr.pop();
console.log(arr);
